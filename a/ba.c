#include <stdio.h>

int main() {
    int p, t, r, i;

    
    scanf("%d", &p);

    
    scanf("%d", &t);

    
    scanf("%d", &r);

    i = (p * t * r) / 100;

    printf("Simple interest = %d", i);

    return 0;
}
