#include <stdio.h>

int main()
{
    int n;
    printf("Enter The Length Of Array:\n");
    scanf("%d",&n);
    int arr[n];
    for (int i = 0; i < n; i++)
    {
        printf("Enter element %d:\n",i+1);
        scanf("%d",&arr[i]);
    }
    for (int i = 0; i < n; i++)
    {
        printf("%d ",arr[i]);
    }
    temp=0;
    for
    
    
    
    return 0;
}